<%= form_for @post, url: post_path, as: 'post' do |f| %>
  <p><%= f.text_field :title, placeholder: 'Title' %></p>
  <p><%= f.text_field :subtitle, placeholder: 'Subtitle' %></p>
  <p><%= f.text_area :body, placeholder: 'Body' %></p>
  <p><%= f.submit %></p>
<% end %>